@model IEnumerable<Friseur.Models.ClientType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientTypeId })
        </td>
    </tr>
}

</table>
